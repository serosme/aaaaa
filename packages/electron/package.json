{
  "name": "electron",
  "type": "module",
  "version": "1.0.0",
  "main": "dist/main.js",
  "scripts": {
    "dev": "tsc -p tsconfig.json && cross-env NODE_ENV=dev electron .",
    "preview": "npm --prefix ../renderer run generate && tsc -p tsconfig.json && electron .",
    "build": "npm --prefix ../renderer run build && tsc -p tsconfig.json && electron-builder --win"
  },
  "dependencies": {
    "electron-store": "^11.0.2"
  },
  "devDependencies": {
    "cross-env": "^10.1.0",
    "electron": "^41.1.1",
    "electron-builder": "^26.8.1",
    "shared": "^1.0.0",
    "typescript": "^6.0.2"
  },
  "build": {
    "appId": "tools",
    "productName": "Tools",
    "directories": {
      "output": "release"
    },
    "electronLanguages": [
      "zh-CN"
    ],
    "asar": false,
    "extraResources": [
      {
        "from": "../renderer/.output/public",
        "to": "renderer/.output/public"
      }
    ],
    "win": {
      "target": "zip"
    }
  }
}
