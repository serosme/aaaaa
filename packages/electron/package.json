{
  "name": "electron",
  "type": "module",
  "main": "dist/main.js",
  "scripts": {
    "dev": "npm --prefix ../shared run build && tsc -p tsconfig.json && electron ."
  },
  "dependencies": {
    "@app/shared": "*"
  },
  "devDependencies": {
    "electron": "^41.1.1",
    "typescript": "^6.0.0"
  }
}
